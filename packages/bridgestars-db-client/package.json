{
  "name": "bridgestars-db-client",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf -rf dist && tsc",
    "preinstall": "cd `git rev-parse --show-toplevel` &&  git submodule update --init --recursive && cd packages/Parse-SDK-TS && npm install && npm run build",
    "dependency-check": "npx madge --circular --extensions ts ./src"
  },
  "keywords": [],
  "author": "Theodor Lundqvist",
  "dependencies": {
    "@types/parse": "^3.0.4",
    "parse": "^4.2.0",
    "parse-sdk-ts": "link:../Parse-SDK-TS",
    "xmlhttprequest": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
